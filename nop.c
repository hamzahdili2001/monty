#include "monty.h"

/**
 * nop - is nop.
 * @stack: stack.
 * @line_number: line number.
 * Return: it does Nothing from the start.
*/
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
